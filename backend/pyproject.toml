[project]
name = "hellodocker-backend"
version = "0.1.0"
description = "Backend service for the HelloDocker full-stack learning project."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]
